{
  "name": "hyperbolic-loyalty",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "npm run dev --workspace=apps/web",
    "build:web": "npm run build --workspace=apps/web",
    "mobile": "npm run start --workspace=apps/mobile"
  },
  "engines": {
    "node": ">=20"
  }
}
